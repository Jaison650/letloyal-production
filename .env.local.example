# Database (Railway MySQL) — used from M1
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=

# Auth
JWT_SECRET=
JWT_EXPIRY=7d

# Public base URL — used by QR generation (M5)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Spend QR signing + expiry (M5)
QR_HMAC_SECRET=
QR_SPEND_EXPIRY_SECONDS=300
